import styled from 'styled-components';

// type CardProps = {
//     newCard: boolean;
// };

type BadgeProps = {
    votes: number;
};

export const FloorStyle = styled.div`
    width: 240px;
    height: 80px;
    padding: 12px;
    border: 1px solid #222;
`;