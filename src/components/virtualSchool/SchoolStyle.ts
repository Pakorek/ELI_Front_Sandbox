import styled from 'styled-components';

// type CardProps = {
//     newCard: boolean;
// };

type BadgeProps = {
    votes: number;
};

export const School = styled.section`
    width: 800px;
    height: auto;
    padding: 20px;
    margin: 0 auto;
`;